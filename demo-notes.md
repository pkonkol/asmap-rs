This branch is supposed to present the last working version. I won't be developing this project again but want to showcase it.
It works with `cargo run -- load-asrank`, takes some time tho. Rust 1.90.0

`cargo install cargo-watch`
`cargo install --locked trunk`
`rustup target add wasm32-unknown-unknown`
`cd asmap && ./dev.sh`
